# Copy to deploy/.env and fill in. Never commit real secrets.
# Generate strong values, e.g.:  openssl rand -hex 32
OPSCTL_AUTH__JWT_SECRET=change-me-to-a-long-random-string
OPSCTL_VAULT__PASSPHRASE=change-me-vault-unseal-passphrase
OPSCTL_BOOTSTRAP__ADMIN_PASSWORD=change-me-admin-password
